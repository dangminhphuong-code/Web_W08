<div class="container">
    <div class="row">
        <div class="col-12 col-md-6">
            <div class="ratio ratio-1x1">
                <img src="/imgs/p{{product.id}}.png" class="img-fluid" alt="{{product.name}}">
            </div>
        </div>
        <div class="col-12 col-md-6">
            <h2>{{product.title}}</h2>
            <p class="mt-3">{{product.description}}</p>
            <h4 class="mt-4">Price: ${{produce.price}}</h4>
            <button class="btn btn-success mt-4">Add to Cart</button>
        </div>
    </div>
</div>